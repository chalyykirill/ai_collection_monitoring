from __future__ import annotations


EVENT_DOC_CONTEXT = {
    "bank_unavailable_day": (
        "Известное событие: единый день недоступности банка (ЕДН). "
        "Для записи с event_type=bank_unavailable_day и "
        "is_expected_event=true календарь мониторинга подтверждает плановое "
        "технологическое окно. В этот день допустимы cnt_clients=0, "
        "cnt_scored=0 и NULL в зависимых метриках. Нужно проверить "
        "восстановление потока на следующем доступном дне."
    ),
    "credit_card_batch_inflow": (
        "Известная особенность процесса: портфель credit_card поступает "
        "неритмичными пакетами. Для записи с "
        "event_type=credit_card_batch_inflow и is_expected_event=true "
        "календарь загрузок подтверждает пакетное поступление. В такой день "
        "рост объема и доли продукта может быть ожидаемой особенностью, но "
        "следует проверить завершение загрузки и метрики без эффекта микса."
    ),
    "model_gini_drop": (
        "Падение Gini не относится к ожидаемым событиям. Оно требует проверки "
        "target rate, PSI score, распределения score, качества признаков и "
        "изменения состава входящего потока. До диагностики root cause не "
        "считается подтвержденным."
    ),
    "empty_feature_vector": (
        "Рост empty_feature_vector_share и feature_missing_rate не является "
        "штатным событием. Сдвиг score при таких признаках может быть связан "
        "с качеством входных данных. Следует проверить витрину признаков, "
        "источники и заполненность feature vector."
    ),
    "new_high_risk_segment": (
        "Одновременный рост avg_score, high_risk_share, target_rate и PSI "
        "может указывать на изменение состава потока. Событие требует "
        "проверки сегментного, продуктового и DPD-микса; причина не считается "
        "подтвержденной без диагностики."
    ),
    "optimizer_mass_reject": (
        "Массовый рост reject_share вместе с падением одобренных коммуникаций "
        "не является штатным режимом. Это потенциальный инцидент, требующий "
        "проверки правил оптимизатора, бюджетных ограничений, входных данных "
        "и справочников коммуникаций."
    ),
}

DEFAULT_DOC_CONTEXT = (
    "Для события нет подтвержденного описания в доступной документации. "
    "Необходимо использовать needs_manual_review при недостатке данных."
)


def get_event_doc_context(event_type: str) -> str:
    return EVENT_DOC_CONTEXT.get(event_type, DEFAULT_DOC_CONTEXT)

